<%# app/views/bible_cultural_phrases/new.html.erb %>
<% content_for :title, "Add Phrase — #{@novel.title} — Hawk" %>

<%= render "layouts/breadcrumb", crumbs: [
      ["Novels", novels_path],
      [@novel.title, novel_path(@novel)],
      ["Bible", novel_bible_path(@novel)],
      ["Cultural Phrases", novel_bible_cultural_phrases_path(@novel)],
      ["Add Phrase", nil]
    ] %>

<div class="page-header">
  <h1 class="page-title">Add Cultural Phrase</h1>
</div>

<div class="card">
  <%= render "form" %>
</div>

<%# app/views/bible_characters/new.html.erb %>
<% content_for :title, "Add Character — #{@novel.title} — Hawk" %>

<%= render "layouts/breadcrumb", crumbs: [
      ["Novels", novels_path],
      [@novel.title, novel_path(@novel)],
      ["Bible", novel_bible_path(@novel)],
      ["Characters", novel_bible_characters_path(@novel)],
      ["Add Character", nil]
    ] %>

<div class="page-header">
  <h1 class="page-title">Add Character</h1>
</div>

<div class="card">
  <%= render "form" %>
</div>
